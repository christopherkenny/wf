% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/register_hook.R
\name{register_hook}
\alias{register_hook}
\title{Register a hook command in the settings file}
\usage{
register_hook(
  event,
  command,
  matcher = NULL,
  timeout = NULL,
  async = FALSE,
  agent = NULL,
  scope = c("project", "local", "global"),
  path = NULL
)
}
\arguments{
\item{event}{The lifecycle event to attach the hook to. One of
\code{"PreToolUse"}, \code{"PostToolUse"}, \code{"UserPromptSubmit"}, \code{"Stop"}, or
\code{"SubagentStop"}.}

\item{command}{The shell command to execute when the hook fires.}

\item{matcher}{An optional tool-name pattern (for \code{"PreToolUse"} and
\code{"PostToolUse"} events) used to filter which tool calls trigger the
hook, e.g. \code{"Bash|Edit"}. When \code{NULL} (the default), the hook applies
to all tool calls for the event.}

\item{timeout}{Optional timeout in seconds for the hook command.}

\item{async}{If \code{TRUE}, the hook runs asynchronously and the agent does
not wait for it to complete. Default is \code{FALSE}.}

\item{agent, scope, path}{Passed to \code{\link[=settings_path]{settings_path()}} to locate the
settings file. Defaults resolve to the project-scope settings of the
detected coding agent.}
}
\value{
The path to the modified settings file, invisibly.
}
\description{
Adds a shell command hook to a coding agent's \code{settings.json} file. Hooks
run automatically on agent events such as \code{"PreToolUse"} or
\code{"UserPromptSubmit"}. To install a hook script from GitHub or a local
file, use \code{\link[=add_hook]{add_hook()}} instead.
}
\examples{
tmp <- tempfile(fileext = '.json')
register_hook('PreToolUse', 'echo hello', path = tmp)
}
