# remove_hook errors if hook not installed

    Code
      remove_hook("ghost", path = tmp)
    Condition
      Error in `remove_hook()`:
      ! Hook "ghost" is not installed at '<path>'.
      i Use `list_hooks()` to see installed hooks.

