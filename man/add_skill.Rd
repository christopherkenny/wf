% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_skill.R
\name{add_skill}
\alias{add_skill}
\title{Install a skill}
\usage{
add_skill(source, path = skill_path(), skill = NULL, overwrite = FALSE)
}
\arguments{
\item{source}{One of:
\itemize{
\item A GitHub URL pointing to a repo, e.g. \code{"https://github.com/owner/repo"}.
\item A GitHub URL pointing to a subdirectory, e.g.
\code{"https://github.com/owner/repo/tree/main/path/to/skill"}.
\item A GitHub shorthand, e.g. \code{"owner/repo"}.
\item A local directory path containing a \code{SKILL.md} file.
}}

\item{path}{The skills directory to install into. Defaults to
\code{\link[=skill_path]{skill_path()}}, which resolves the agent from \code{WF_AGENT}, a directory
scan, or falls back to \code{"claude_code"}.}

\item{skill}{For multi-skill repositories that store skills under a
\verb{skills/} subdirectory, the name of the skill to install, e.g.
\code{skill = "proofread"}. When supplied, the skill is read from
\verb{skills/<skill>} within the repository. Ignored when \code{source} already
points to a specific subdirectory via \verb{/tree/...}.}

\item{overwrite}{If \code{FALSE} (the default), an error is raised if the skill
is already installed. Set to \code{TRUE} to replace it.}
}
\value{
The path to the installed skill directory, invisibly.
}
\description{
Installs a skill from a GitHub repository or a local directory into a skills
directory.
}
\examples{
src <- tempfile()
dir.create(src)
writeLines(
  c('---', 'name: example', 'description: An example skill.', '---'),
  file.path(src, 'SKILL.md')
)
add_skill(src, tempfile())
}
