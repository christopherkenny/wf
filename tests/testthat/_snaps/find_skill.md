# find_skill errors without query

    Code
      find_skill()
    Condition
      Error in `find_skill()`:
      ! `query` must not be missing.

# find_skill errors on network failure

    Code
      find_skill("rstats")
    Condition
      Error in `gh_search_repos()`:
      ! network error

