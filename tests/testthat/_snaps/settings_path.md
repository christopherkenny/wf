# settings_path errors on unknown agent

    Code
      settings_path("unknown_agent")
    Condition
      Error in `resolve_hook_settings_path()`:
      ! `agent` must be one of "claude_code", "openclaw", "codex", "cursor", "gemini_cli", "github_copilot", or "posit_ai".
      x Got "unknown_agent".

