% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill_path.R
\name{skill_path}
\alias{skill_path}
\title{Get the conventional skill path for an agent}
\usage{
skill_path(agent = NULL, scope = c("project", "global"))
}
\arguments{
\item{agent}{One of \code{"claude_code"} (or its alias \code{"claude"}),
\code{"openclaw"}, \code{"codex"}, \code{"cursor"}, \code{"gemini_cli"},
\code{"github_copilot"} (or its alias \code{"copilot"}), or \code{"posit_ai"} (or its
alias \code{"posit"}). If
\code{NULL} (the default), the agent is resolved in
order: (1) the \code{WF_AGENT} environment variable, (2) a scan of the current
working directory for a recognised agent config folder (\code{.claude},
\code{.cursor}, etc.), and (3) a final fallback to \code{"claude_code"}. Set
\code{WF_AGENT} in your \code{.Renviron} (e.g. with \code{\link[usethis:edit]{usethis::edit_r_environ()}})
to avoid passing \code{agent} every time.}

\item{scope}{One of \code{"project"} (a path relative to the current working
directory, suitable for committing to version control) or \code{"global"} (a
path in the user's home directory, available across all projects).}
}
\value{
A length-1 character vector giving the conventional skill path.
}
\description{
Returns the conventional directory path where skills for a given agent are
stored. The path is not expanded (i.e., \code{~} is not resolved to the home
directory). Use \code{\link[fs:path_expand]{fs::path_expand()}} if you need an absolute path.
}
\examples{
skill_path('claude_code', 'project')
skill_path('claude', 'project') # alias for claude_code
skill_path('github_copilot', 'project')
skill_path('copilot', 'project') # alias for github_copilot
skill_path('posit_ai', 'project')
skill_path('posit', 'project') # alias for posit_ai
skill_path('cursor', 'global')
skill_path() # auto-detects from WF_AGENT, dir scan, or falls back to claude_code
}
