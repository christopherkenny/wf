% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings_path.R
\name{settings_path}
\alias{settings_path}
\title{Get the path to a coding agent's settings file}
\usage{
settings_path(agent = NULL, scope = c("project", "local", "global"))
}
\arguments{
\item{agent}{One of \code{"claude_code"} (or its alias \code{"claude"}),
\code{"openclaw"}, \code{"codex"}, \code{"cursor"}, \code{"gemini_cli"},
\code{"github_copilot"} (or its alias \code{"copilot"}), or \code{"posit_ai"} (or its
alias \code{"posit"}). If \code{NULL} (the default), the agent is resolved in
order: (1) the \code{WF_AGENT} environment variable, (2) a scan of the
current working directory for a recognised agent config folder
(\code{.claude}, \code{.cursor}, etc.), and (3) a final fallback to
\code{"claude_code"}. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid passing \code{agent} every time.}

\item{scope}{One of:
\itemize{
\item \code{"project"}: \verb{.\{agent\}/settings.json} in the current directory.
\item \code{"local"}: \verb{.\{agent\}/settings.local.json} (gitignored, for personal
overrides).
\item \code{"global"}: \verb{~/.\{agent\}/settings.json}.
}}
}
\value{
A length-1 character vector giving the path to the settings file.
}
\description{
Returns the path to the \code{settings.json} file where hooks are configured
for a given coding agent and scope. The path is not expanded (i.e., \code{~}
is not resolved to the home directory). Use \code{\link[fs:path_expand]{fs::path_expand()}} if you
need an absolute path.
}
\examples{
settings_path('claude_code', 'project')
settings_path('claude', 'local') # alias for claude_code
settings_path('cursor', 'global')
}
