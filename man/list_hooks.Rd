% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_hooks.R
\name{list_hooks}
\alias{list_hooks}
\title{List hooks in a settings file}
\usage{
list_hooks(
  path = NULL,
  settings = NULL,
  agent = NULL,
  scope = c("project", "local", "global")
)
}
\arguments{
\item{path}{The hooks directory. When supplied, the \code{file} column in the
returned data frame will contain the path to the installed script file
for hooks that were installed with \code{\link[=add_hook]{add_hook()}}. Can be one of:
\itemize{
\item A known coding agent name to use that agent's conventional hooks path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case \code{file} will be \code{NA} for all rows.
}}

\item{settings}{Path to the \code{settings.json} file to read. When \code{NULL}
(the default), resolved from \code{agent}, \code{scope}, and the \code{WF_AGENT}
environment variable.}

\item{agent, scope}{Passed to \code{\link[=settings_path]{settings_path()}} to locate the settings
file when \code{settings} is \code{NULL}. Defaults resolve to the project-scope
settings of the detected coding agent.}
}
\value{
A data frame with columns:
\itemize{
\item \code{event}: the lifecycle event name (e.g. \code{"PreToolUse"}).
\item \code{matcher}: the tool-name pattern, or \code{NA} if none.
\item \code{command}: the shell command to execute.
\item \code{file}: path to the installed script file, or \code{NA} if not tracked.
}
}
\description{
Returns a data frame of all hooks configured in a coding agent's
\code{settings.json} file.
}
\examples{
tmp <- tempfile(fileext = '.json')
register_hook('PreToolUse', 'echo hello', path = tmp)
list_hooks(settings = tmp)
}
