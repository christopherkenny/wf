% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_hooks.R
\name{check_hooks}
\alias{check_hooks}
\title{Check installed hooks for available updates}
\usage{
check_hooks(path = NULL)
}
\arguments{
\item{path}{The hooks directory. Can be one of:
\itemize{
\item A known coding agent name such as \code{'claude_code'}, \code{'cursor'}, or
\code{'github_copilot'} (see \code{\link[=hook_path]{hook_path()}} for the full list) to use
that agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}
}
\value{
A data frame with columns:
\itemize{
\item \code{name}: hook name.
\item \code{installed_sha}: the SHA recorded at install time (\code{NA} for local).
\item \code{latest_sha}: the current HEAD SHA on GitHub (\code{NA} for local or on
network failure).
\item \code{update_available}: \code{TRUE} if installed and latest SHAs differ.
}
}
\description{
Compares each installed hook's recorded commit SHA against the latest
commit on GitHub. Local hooks are reported as not updatable.
}
\examples{
check_hooks(tempfile())
}
