% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_path.R
\name{agent_path}
\alias{agent_path}
\title{Get the conventional agent path for a coding agent}
\usage{
agent_path(agent = NULL, scope = c("project", "global"))
}
\arguments{
\item{agent}{One of \code{"claude_code"} (or its alias \code{"claude"}),
\code{"openclaw"}, \code{"codex"}, \code{"cursor"}, \code{"gemini_cli"},
\code{"github_copilot"} (or its alias \code{"copilot"}), or \code{"posit_ai"} (or its
alias \code{"posit"}). If \code{NULL} (the default), the agent is resolved in
order: (1) the \code{WF_AGENT} environment variable, (2) a scan of the
current working directory for a recognised agent config folder
(\code{.claude}, \code{.cursor}, etc.), and (3) a final fallback to
\code{"claude_code"}. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid passing \code{agent} every time.}

\item{scope}{One of \code{"project"} (a path relative to the current working
directory, suitable for committing to version control) or \code{"global"} (a
path in the user's home directory, available across all projects).}
}
\value{
A length-1 character vector giving the conventional agents path.
}
\description{
Returns the conventional directory path where custom agents for a given
coding agent are stored. The path is not expanded (i.e., \code{~} is not
resolved to the home directory). Use \code{\link[fs:path_expand]{fs::path_expand()}} if you need an
absolute path.
}
\examples{
agent_path('claude_code', 'project')
agent_path('claude', 'project') # alias for claude_code
agent_path('cursor', 'global')
agent_path() # auto-detects from WF_AGENT, dir scan, or falls back to claude_code
}
