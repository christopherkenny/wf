% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_rule.R
\name{init_rule}
\alias{init_rule}
\title{Create a new rule template}
\usage{
init_rule(name, path = NULL)
}
\arguments{
\item{name}{Rule name. Must be 1-64 characters, lowercase alphanumeric
with single hyphens (no consecutive \verb{--}), and cannot start or end with
a hyphen. Consider a descriptive noun form (e.g. \code{"testing"},
\code{"code-style"}).}

\item{path}{Directory in which to create the rule file. Can be one of:
\itemize{
\item A known coding agent name such as \code{"claude_code"} or \code{"github_copilot"}
to use that agent's conventional project-scope path (see \code{\link[=rule_path]{rule_path()}}
for the full list).
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}
}
\value{
The path to the new rule file, invisibly.
}
\description{
Creates a new rule file at \code{path/name.md} containing a template ready to
be filled in.
}
\examples{
init_rule('my-rule', tempfile())
}
