% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_agents.R
\name{list_agents}
\alias{list_agents}
\title{List installed agents}
\usage{
list_agents(path = NULL)
}
\arguments{
\item{path}{The agents directory. Can be one of:
\itemize{
\item A known coding agent name such as \code{'claude_code'}, \code{'cursor'}, or
\code{'github_copilot'} (see \code{\link[=agent_path]{agent_path()}} for the full list) to use
that agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}
}
\value{
A data frame with columns:
\itemize{
\item \code{name}: agent name from frontmatter (or filename stem).
\item \code{description}: agent description from frontmatter.
\item \code{source}: the source URL or local path the agent was installed from.
\item \code{installed_at}: ISO 8601 timestamp of when the agent was installed.
}
}
\description{
Returns a data frame describing all agents installed in an agents directory.
}
\examples{
list_agents(tempfile())
}
