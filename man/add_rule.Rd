% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_rule.R
\name{add_rule}
\alias{add_rule}
\title{Install a rule}
\usage{
add_rule(source, rule = NULL, path = NULL, overwrite = FALSE)
}
\arguments{
\item{source}{One of:
\itemize{
\item A GitHub URL pointing to a repo, e.g. \code{"https://github.com/owner/repo"}.
\item A GitHub URL pointing to a subdirectory or file, e.g.
\code{"https://github.com/owner/repo/tree/main/path/to/rule.md"}.
\item A GitHub shorthand, e.g. \code{"owner/repo"}.
\item A local file path pointing to a Markdown file.
}}

\item{rule}{For multi-rule repositories that store rules under a \verb{rules/}
subdirectory, the name of the rule to install (without the \code{.md}
extension), e.g. \code{rule = "testing"}. When supplied, the rule is read
from \verb{rules/<rule>.md} within the repository. Ignored when \code{source}
already points to a specific path via \verb{/tree/...}.}

\item{path}{The rules directory. Can be one of:
\itemize{
\item A known coding agent name such as \code{'claude_code'}, \code{'cursor'}, or
\code{'github_copilot'} (see \code{\link[=rule_path]{rule_path()}} for the full list) to use
that agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}

\item{overwrite}{If \code{FALSE} (the default), an error is raised if the rule is already installed. Set to \code{TRUE} to replace it.}
}
\value{
The path to the installed rule file, invisibly.
}
\description{
Installs a rule from a GitHub repository or a local file into a rules
directory. Rules are Markdown files with optional YAML frontmatter. The
rule \code{name} comes from the frontmatter if present, otherwise from the
filename stem.
}
\examples{
src <- tempfile(fileext = '.md')
writeLines(
  c('---', 'name: example', 'description: An example rule.', '---'),
  src
)
add_rule(src, path = tempfile())
}
