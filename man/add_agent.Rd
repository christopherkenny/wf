% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_agent.R
\name{add_agent}
\alias{add_agent}
\title{Install an agent}
\usage{
add_agent(source, agent = NULL, path = NULL, overwrite = FALSE)
}
\arguments{
\item{source}{One of:
\itemize{
\item A GitHub URL pointing to a repo, e.g. \code{"https://github.com/owner/repo"}.
\item A GitHub URL pointing to a subdirectory or file, e.g.
\code{"https://github.com/owner/repo/tree/main/path/to/agent.md"}.
\item A GitHub shorthand, e.g. \code{"owner/repo"}.
\item A local file path pointing to a Markdown file.
}}

\item{agent}{For multi-agent repositories that store agents under an
\verb{agents/} subdirectory, the name of the agent to install (without the
\code{.md} extension), e.g. \code{agent = "code-reviewer"}. When supplied, the
agent is read from \verb{agents/<agent>.md} within the repository. Ignored
when \code{source} already points to a specific path via \verb{/tree/...}.}

\item{path}{The agents directory. Can be one of:
\itemize{
\item A known coding agent name such as \code{'claude_code'}, \code{'cursor'}, or
\code{'github_copilot'} (see \code{\link[=agent_path]{agent_path()}} for the full list) to use
that agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}

\item{overwrite}{If \code{FALSE} (the default), an error is raised if the agent is already installed. Set to \code{TRUE} to replace it.}
}
\value{
The path to the installed agent file, invisibly.
}
\description{
Installs a custom agent from a GitHub repository or a local file into an
agents directory. Agents are single Markdown files with YAML frontmatter
specifying at minimum a \code{name} and \code{description}.
}
\examples{
src <- tempfile(fileext = '.md')
writeLines(
  c('---', 'name: example', 'description: An example agent.', '---'),
  src
)
add_agent(src, path = tempfile())
}
