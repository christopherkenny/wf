% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_agent.R
\name{remove_agent}
\alias{remove_agent}
\title{Remove an installed agent}
\usage{
remove_agent(name, path = NULL, force = FALSE)
}
\arguments{
\item{name}{The name of the agent to remove (without the \code{.md} extension).}

\item{path}{The agents directory where the agent is installed. Can be one
of:
\itemize{
\item A known coding agent name such as \code{"claude_code"}, \code{"cursor"}, or
\code{"github_copilot"} (see \code{\link[=agent_path]{agent_path()}} for the full list) to use that
agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}

\item{force}{If \code{FALSE} (the default), prompts for confirmation in
interactive sessions. Set to \code{TRUE} to skip the prompt.}
}
\value{
The name of the removed agent, invisibly.
}
\description{
Deletes an agent file from an agents directory and removes it from the
lock file.
}
\examples{
src <- tempfile(fileext = '.md')
writeLines(
  c('---', 'name: example', 'description: An example agent.', '---'),
  src
)
tmp <- tempfile()
add_agent(src, path = tmp)
remove_agent('example', tmp, force = TRUE)
}
