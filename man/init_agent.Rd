% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_agent.R
\name{init_agent}
\alias{init_agent}
\title{Create a new agent template}
\usage{
init_agent(name, path = NULL)
}
\arguments{
\item{name}{Agent name. Must be 1-64 characters, lowercase alphanumeric
with single hyphens (no consecutive \verb{--}), and cannot start or end with
a hyphen. Consider using a gerund or role form (e.g.
\code{"code-reviewer"}, \code{"test-writer"}).}

\item{path}{Directory in which to create the agent file. Can be one of:
\itemize{
\item A known coding agent name such as \code{"claude_code"} or \code{"github_copilot"}
to use that agent's conventional project-scope path (see \code{\link[=agent_path]{agent_path()}}
for the full list).
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}
}
\value{
The path to the new agent file, invisibly.
}
\description{
Creates a new agent file at \code{path/name.md} containing a template ready to
be filled in.
}
\examples{
init_agent('my-agent', tempfile())
}
