% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_hook.R
\name{init_hook}
\alias{init_hook}
\title{Create a new hook template}
\usage{
init_hook(name, path = NULL)
}
\arguments{
\item{name}{Hook name. Must be 1-64 characters, lowercase alphanumeric
with single hyphens (no consecutive \verb{--}), and cannot start or end
with a hyphen. Consider using a kebab-case verb form (e.g.
\code{"lint-on-save"}, \code{"check-format"}).}

\item{path}{Directory in which to create the hook file. Can be one of:
\itemize{
\item A known coding agent name such as \code{"claude_code"} or
\code{"github_copilot"} to use that agent's conventional project-scope
path (see \code{\link[=hook_path]{hook_path()}} for the full list).
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}
}
\value{
The path to the new hook file, invisibly.
}
\description{
Creates a new shell script hook file at \code{path/name.sh} containing a
minimal template ready to be filled in. After editing the script,
register it with \code{\link[=register_hook]{register_hook()}} or install it with \code{\link[=add_hook]{add_hook()}}.
}
\examples{
init_hook('my-hook', tempfile())
}
