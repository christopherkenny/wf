# hook_path errors on unknown agent

    Code
      hook_path("unknown_agent")
    Condition
      Error in `hook_path()`:
      ! `agent` must be one of "claude_code", "openclaw", "codex", "cursor", "gemini_cli", "github_copilot", or "posit_ai".
      x Got "unknown_agent".

