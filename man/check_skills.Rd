% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_skills.R
\name{check_skills}
\alias{check_skills}
\title{Check installed skills for available updates}
\usage{
check_skills(path = skill_path())
}
\arguments{
\item{path}{The skills directory to check. Defaults to \code{\link[=skill_path]{skill_path()}},
which resolves the agent from \code{WF_AGENT}, a directory scan, or falls
back to \code{"claude_code"}.}
}
\value{
A data frame with columns:
\itemize{
\item \code{name}: skill name.
\item \code{installed_sha}: the SHA recorded at install time (\code{NA} for local).
\item \code{latest_sha}: the current HEAD SHA on GitHub (\code{NA} for local or on
network failure).
\item \code{update_available}: \code{TRUE} if installed and latest SHAs differ.
}
}
\description{
Compares each installed skill's recorded commit SHA against the latest
commit on GitHub. Local skills are reported as not updatable.
}
\examples{
check_skills(tempfile())
}
