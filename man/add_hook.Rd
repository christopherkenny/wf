% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_hook.R
\name{add_hook}
\alias{add_hook}
\title{Install a hook script}
\usage{
add_hook(
  source,
  event,
  hook = NULL,
  matcher = NULL,
  path = NULL,
  settings = NULL,
  overwrite = FALSE,
  timeout = NULL,
  async = FALSE
)
}
\arguments{
\item{source}{One of:
\itemize{
\item A GitHub URL pointing to a repo, e.g. \code{"https://github.com/owner/repo"}.
\item A GitHub shorthand, e.g. \code{"owner/repo"}.
\item A local file path pointing to an executable script (\code{.sh}, \code{.R},
\code{.py}, etc.).
}}

\item{event}{The lifecycle event to attach the hook to. One of
\code{"PreToolUse"}, \code{"PostToolUse"}, \code{"UserPromptSubmit"}, \code{"Stop"}, or
\code{"SubagentStop"}.}

\item{hook}{For multi-hook repositories that store scripts under a
\verb{hooks/} subdirectory, the name of the hook to install (without the
file extension), e.g. \code{hook = "lint-staged"}. When supplied, the script
is read from \verb{hooks/<hook>.<ext>} within the repository. Ignored when
\code{source} already points to a specific file.}

\item{matcher}{An optional tool-name pattern (for \code{"PreToolUse"} and
\code{"PostToolUse"} events) used to filter which tool calls trigger the
hook, e.g. \code{"Bash|Edit"}. When \code{NULL} (the default), the hook applies
to all tool calls for the event.}

\item{path}{The hooks directory. Can be one of:
\itemize{
\item A known coding agent name such as \code{'claude_code'}, \code{'cursor'}, or
\code{'github_copilot'} (see \code{\link[=hook_path]{hook_path()}} for the full list) to use
that agent's conventional project-scope path.
\item A character string giving the directory path directly.
\item \code{NULL} (the default), in which case the path is resolved from the
\code{WF_AGENT} environment variable, or by prompting in interactive
sessions. Set \code{WF_AGENT} in your \code{.Renviron} (e.g. with
\code{\link[usethis:edit]{usethis::edit_r_environ()}}) to avoid the prompt.
}}

\item{settings}{Path to the \code{settings.json} file where the hook will be
registered. When \code{NULL} (the default), defaults to
\code{settings.json} in the parent directory of \code{path} (e.g. if \code{path} is
\code{.claude/hooks}, uses \code{.claude/settings.json}).}

\item{overwrite}{If \code{FALSE} (the default), an error is raised if the hook is already installed. Set to \code{TRUE} to replace it.}

\item{timeout}{Optional timeout in seconds for the hook command.}

\item{async}{If \code{TRUE}, the hook runs asynchronously. Default is \code{FALSE}.}
}
\value{
The path to the installed hook script, invisibly.
}
\description{
Installs a hook script from a GitHub repository or a local file into a
hooks directory, then registers it in the corresponding \code{settings.json}
so it runs on the specified lifecycle event.
}
\examples{
tmp_hook <- tempfile(fileext = '.sh')
writeLines(c('#!/bin/bash', 'echo hello'), tmp_hook)
tmp_dir <- tempfile()
tmp_settings <- tempfile(fileext = '.json')
add_hook(tmp_hook,
  event = 'PreToolUse', path = tmp_dir,
  settings = tmp_settings
)
}
